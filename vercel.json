{
  "buildCommand": "cd property-finder-ui && npm install && npm run build",
  "outputDirectory": "./property-finder-ui/dist",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
